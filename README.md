First of all run npm start at main directory to start React server.

After start MongoDB by running mongod at your terminal.

Last step open back-end directory and run "nodemon server" to start back-end.
